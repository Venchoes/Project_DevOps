{
  "name": "projeto-ci-cd",
  "version": "1.0.0",
  "main": "src/index.js",
  "scripts": {
    "test": "jest --passWithNoTests",
    "build": "npm run test && next build"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
